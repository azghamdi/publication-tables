# تقرير مراجعة الوصول — WCAG 2.2 AA

**المنتج:** نشرة الرقم القياسي لأسعار العقارات — الربع الثاني 2026  
**تاريخ المراجعة:** 15 أغسطس 2026

## النطاق

شملت المراجعة: رأس الصفحة، التنقل، تبويبات العرض، فلاتر الجداول، ترتيب الأعمدة، ترقيم الصفحات، الرسومات التفاعلية، أداة المقارنة، التنزيل، المنهجية والتذييل.

## التحسينات المطبقة

- رابط تجاوز مباشر إلى المحتوى الرئيسي.
- معالم دلالية وعناوين وتسميات عربية واضحة.
- نمط تركيز مرئي عالي الوضوح لجميع العناصر التفاعلية.
- نمط تبويبات يدعم الأسهم ولوحة المفاتيح مع `aria-selected` و`tabindex` متزامنين.
- عناوين أعمدة دلالية، وعناوين صفوف باستخدام `scope="row"`.
- عنوان وصفي مخفي للجدول وتعليمات لقارئ الشاشة.
- حالة نتائج الجدول باستخدام `role="status"` و`aria-live`.
- تعريفات ووحدات مرتبطة برمجيًا بعناوين الأعمدة باستخدام `aria-describedby`.
- أسماء بديلة للرموز والصور، مع إخفاء الزخرفي منها عن قارئ الشاشة.
- أهداف لمس مناسبة، ودعم `prefers-reduced-motion`، وتمرير واضح للمحتوى العريض.

## نتيجة المراجعة البنيوية

لم تظهر في النسخة النهائية عوائق بنيوية معروفة تمنع التنقل الأساسي بلوحة المفاتيح أو قراءة الجداول دلاليًا. صُممت التحسينات لاستهداف مستوى WCAG 2.2 AA، وبالأخص: 1.3.1، 1.3.2، 1.4.3، 1.4.10، 1.4.11، 2.1.1، 2.1.2، 2.4.1، 2.4.3، 2.4.7، 2.4.11، 2.5.8، 3.2.1، 3.3.2، 4.1.2، 4.1.3.

## سيناريوهات التحقق اليدوي المطلوبة قبل الاعتماد الرسمي

لا تُعد مراجعة الشفرة وحدها شهادة مطابقة رسمية. قبل اعتماد عبارة «متوافق WCAG 2.2 AA» يجب تنفيذ جلسة بشرية موثقة على النسخة المنشورة تشمل:

1. NVDA مع Firefox أو Chrome على Windows.
2. VoiceOver مع Safari على iOS وmacOS.
3. التنقل الكامل باستخدام Tab وShift+Tab والأسهم وEnter وSpace وEscape فقط.
4. التكبير حتى 200% وإعادة تدفق المحتوى عند عرض 320 CSS px.
5. فحص آلي باستخدام axe-core أو Lighthouse، ثم مراجعة النتائج يدويًا.
6. التحقق من التباين الفعلي بعد النشر ومن جميع حالات hover وfocus وdisabled.

السبب: سلوك قارئ الشاشة يعتمد على نظام التشغيل والمتصفح وإصدار التقنية المساعدة، ولا يمكن إثباته رسميًا من فحص ملف HTML ساكن فقط.
